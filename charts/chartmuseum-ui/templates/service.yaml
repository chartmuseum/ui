apiVersion: v1
kind: Service
metadata:
  name: {{ include "chartmuseum-ui.fullname" . }}
  labels:
    {{- include "chartmuseum-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chartmuseum-ui.selectorLabels" . | nindent 4 }}
